% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-report.R
\name{add_section}
\alias{add_section}
\title{Add a content section to a report}
\usage{
add_section(
  .report,
  id = "",
  title = "",
  description = "",
  report_data,
  txt.fn = NULL,
  tbl.fn = NULL,
  fig.fn = NULL,
  .by = NULL
)
}
\arguments{
\item{.report}{A named analysis grid (e.g. an analyzed decision grid), or
the result of a previous \code{add_section()} call to append to. \code{add_section()}
re-evaluates against the grid by name, so the grid must be a named object
in the calling environment.}

\item{id}{A short identifier for the section, used later to lay it out and
assemble it (e.g. \code{"robustness"}). Should be unique within a report.}

\item{title, description}{Section heading text. Processed with \code{\link[glue:glue]{glue::glue()}},
so they may interpolate \code{.by} columns to vary per subsection.}

\item{report_data}{A code expression — usually a \code{\link[=compose_view]{compose_view()}} call —
applied to the grid to gather this section's data into a single data frame.
This data frame is what each content function receives.}

\item{txt.fn, tbl.fn, fig.fn}{Content functions, each taking the \code{report_data}
data frame as its first argument and returning one rendered object:
\code{txt.fn} a text string, \code{tbl.fn} a table (\code{data.frame}, \code{gt}, or
\code{flextable}), \code{fig.fn} a figure (\code{ggplot} or \code{patchwork}). Must be written
as a call (with parentheses), since the data is piped in. Any may be
\code{NULL}; an omitted content type yields an empty slot and is skipped when
the section is rendered.}

\item{.by}{Columns that split the section into subsections, one per unique
combination. When \code{NULL}, the section is a single unit.}
}
\value{
A report grid (a tibble) with one row per subsection. Each row
carries the section's id, the per-subsection title and description, and the
content-generating code for each channel in the \code{sec_txt}, \code{sec_tbl}, and
\code{sec_fig} columns — full pipelines from the analysis grid through the
content function, stored as code and evaluated only when the section is
previewed or generated. Empty channels hold \code{"NULL"}. The originating grid
name is recorded as an \code{"analysis_grid"} attribute so successive
\code{add_section()} calls can chain.
}
\description{
Defines a section of a report by attaching content-generating code to an
analysis grid. A section bundles up to three kinds of content — text, a
table, and a figure — each produced by a function applied to the section's
gathered data. Sections accumulate: piping one \code{add_section()} into the next
builds a multi-section report grid.
}
\details{
\code{add_section()} records the content functions (\code{txt.fn}, \code{tbl.fn}, \code{fig.fn})
as code; it does not run them. Realization is deferred to the moment a
section is previewed (\code{\link[=preview_section]{preview_section()}}) or the document is generated
(\code{\link[=generate_docs]{generate_docs()}}), so defining a section is cheap no matter how many
subsections it fans out into. The stored code \emph{is} the section — it is read
back when inspecting, and evaluated when rendering.

\code{add_section()} stores code, not rendered output. The \code{sec_txt}, \code{sec_tbl},
and \code{sec_fig} columns each hold a complete pipeline — the analysis grid,
through \code{report_data}, through the content function — as an evaluable string.
Nothing is rendered here; realization happens downstream. This is the
package's code-as-artifact principle in its strongest form: a section is
fully described by readable, re-runnable code, inspectable with
\code{\link[=show_section_content]{show_section_content()}} and rendered only at preview or generation time.

Deferring realization also keeps \code{add_section()} fast: a section fanned out
into many subsections costs no rendering at definition time, since the
content is rendered later — once — when the document is built.

Content functions have access only to the gathered section data, not to the
section's metadata fields — a figure title, for instance, belongs inside
\code{fig.fn}, while a section- or slide-level heading belongs in \code{title}.
}
\section{Section content}{
 gather, then distil:
A section is built in two steps. First, \code{report_data} gathers the data the
section needs from the grid — typically a \code{\link[=compose_view]{compose_view()}} call that joins
the relevant unpacked results into a single data frame. Second, each content
function (\code{txt.fn}, \code{tbl.fn}, \code{fig.fn}) receives that gathered data frame as
its first argument and distils it into one rendered object: a text string, a
table, or a figure.

So the flow is always: \code{report_data} produces the section's data frame, and
each content function consumes that same data frame. A content function is
written as code that takes the \code{compose_view} output as its first argument —
for example \code{fig.fn = ggplot(aes(...)) + geom_point()}, or a call to a named
function whose first argument is the data, written with parentheses as
\code{fig.fn = make_spec_curve()} so it sits correctly after the pipe. Because the
data is piped into the content function, the function must be written as a
call (with parentheses), not a bare name.

A section need not have all three content types. Any of \code{txt.fn}, \code{tbl.fn},
or \code{fig.fn} may be left \code{NULL}, in which case that content slot is empty and
simply omitted wherever the section is rendered. A figure-only section, a
table-plus-text section, or any combination is valid.
}

\section{Sections and subsections}{

When \code{.by} is supplied, the section is \emph{fanned out} into one subsection per
unique combination of the \code{.by} columns. Each subsection receives its own
filtered slice of the grid and its own title and description. Because
\code{title} and \code{description} are processed with \code{\link[glue:glue]{glue::glue()}}, they can
reference the \code{.by} columns to produce per-subsection labels — for example,
\code{title = "Results for {outcome}"} yields a distinct title per \code{outcome}.

Without \code{.by}, the section is a single unit with one title and description,
and glue interpolation of the \code{.by} columns does not apply (there are no
subsections to vary over).
}

\examples{
\dontrun{
# A figure-only section, fanned out by outcome
report <-
  analyzed_grid |>
  add_section(
    id          = "estimates",
    title       = "Effect estimates for {outcome}",
    description = "Distribution across specifications",
    report_data = compose_view(model_parameters, model_performance),
    fig.fn      = make_spec_curve(),
    .by         = outcome
  )

# Chain a table-only section onto the same report
report <-
  report |>
  add_section(
    id          = "robustness",
    title       = "Robustness summary",
    report_data = compose_view(rob = assess_robustness),
    tbl.fn      = gt::gt()
  )
}

}
\seealso{
\code{\link[=compose_view]{compose_view()}} for gathering results for section reporting;
\code{\link[=show_section_content]{show_section_content()}} to inspect one section's content code and render it;
\code{\link[=preview_section]{preview_section()}} to preview a section's composed layout;
\code{\link[=layout_section]{layout_section()}} and \code{\link[=generate_docs]{generate_docs()}} to assemble sections into a document.
}
