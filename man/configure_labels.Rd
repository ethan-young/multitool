% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-report.R
\name{configure_labels}
\alias{configure_labels}
\title{Configure human-readable labels for pipeline decisions}
\usage{
configure_labels(.results, ...)
}
\arguments{
\item{.results}{An expanded or analyzed decision grid carrying a
\code{"pipeline"} attribute (or an existing \code{"labels"} attribute from a prior
call).}

\item{...}{Named overrides of the form \code{group = "Label"} or
\code{code = "Label"}. Names matching a group update the group label (and
cascade to its do-nothing filter); names matching a code update that
alternative's label. Names matching neither are skipped with a warning.}
}
\value{
\code{.results}, unchanged except for an updated \code{"labels"} attribute.
}
\description{
Attaches or updates a table of display labels for the groups and
alternatives in a decision pipeline. Labels are stored as a \code{"labels"}
attribute on the object and are used downstream wherever decisions need
human-readable names rather than raw code.
}
\details{
On first use, \code{configure_labels()} derives a default label table from the
object's \code{"pipeline"} attribute: each group is labelled with its own name,
and each alternative with its code. Do-nothing filters (detected by the
\verb{\%in\% unique} pattern) are given the label \code{"No filter on {group}"}, and
model alternatives are labelled with their group name. Subsequent calls
update this table.

Overrides are supplied as named arguments in \code{...}, where each name is a
group or a code and each value is the desired label. Relabelling a group
also cascades to that group's do-nothing filter label, so renaming a group
keeps its "No filter on ..." alternative consistent automatically.

Keys containing spaces must be back-quoted when passed in \code{...}, e.g.
\code{`my group` = "My Group"}. Unmatched keys do not error; they emit a
warning and are ignored, so a typo in one label does not abort the call.
}
\examples{
\dontrun{
results |>
  configure_labels(
    covariates = "Covariate set",
    `iv ~ dv`  = "Unadjusted model"
  )
}

}
\seealso{
\code{\link[=show_labels]{show_labels()}} to inspect the current label table or print a
ready-to-edit \code{configure_labels()} call.
}
