% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-report.R
\name{show_labels}
\alias{show_labels}
\title{Inspect or scaffold pipeline labels}
\usage{
show_labels(.object, .code = FALSE)
}
\arguments{
\item{.object}{An object carrying a \code{"labels"} attribute (i.e. one that has
been passed through \code{\link[=configure_labels]{configure_labels()}}).}

\item{.code}{If \code{FALSE} (default), print the label table. If \code{TRUE}, print
a \code{configure_labels()} call listing every group and alternative as an
editable starting point.}
}
\value{
\code{.object}, invisibly and unchanged. Called for its printed output.
}
\description{
Prints the current label table attached to an object by
\code{\link[=configure_labels]{configure_labels()}}, or — with \code{.code = TRUE} — prints a ready-to-edit
\code{configure_labels()} call pre-filled with every group and alternative, so
the labels can be customized by editing and re-running rather than typed
from scratch.
}
\details{
In \code{.code = TRUE} mode the output is split into a "Groups" block and an
"Alternatives" block. Do-nothing filters are omitted from the alternatives
block, since their labels are managed automatically by the group cascade in
\code{\link[=configure_labels]{configure_labels()}}. Keys containing spaces are back-quoted so the printed
call is valid to paste back in.
}
\examples{
\dontrun{
results |> configure_labels() |> show_labels()

# Print an editable scaffold of every label:
results |> configure_labels() |> show_labels(.code = TRUE)
}

}
\seealso{
\code{\link[=configure_labels]{configure_labels()}} to set the labels this function displays.
}
