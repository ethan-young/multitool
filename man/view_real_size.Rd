% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-report.R
\name{view_real_size}
\alias{view_real_size}
\title{Preview a figure at its true output size}
\usage{
view_real_size(
  .fig,
  mode = "figure",
  width = NULL,
  height = NULL,
  asp_ratio = "wide",
  anchor_height = 7.5,
  design = NULL,
  dpi = 96,
  margin = NULL,
  frame = TRUE,
  give_dims = TRUE,
  title = NULL,
  subtitle = NULL,
  meta_pt_sizes = c(24, 16)
)
}
\arguments{
\item{.fig}{A \code{ggplot} or \code{patchwork} object to preview.}

\item{mode}{\code{"figure"} (default) to render at an exact \code{width}/\code{height}, or
\code{"slide"} to place the figure on a slide canvas and estimate its
dimensions.}

\item{width, height}{Figure dimensions in inches. Required in \code{"figure"} mode;
ignored in \code{"slide"} mode (the canvas is derived from \code{asp_ratio}).}

\item{asp_ratio}{Slide aspect ratio in \code{"slide"} mode: \code{"wide"} (16:9,
default) or \code{"full"} (4:3).}

\item{anchor_height}{The slide height in inches in \code{"slide"} mode (default
\code{7.5}); the width is this times the aspect ratio.}

\item{design}{A patchwork layout design string placing the figure on the
slide (e.g. \code{"A"} for full-bleed, \code{"#AA"} to occupy the right two-thirds
with reserved blank space). \code{"slide"} mode only; defaults to \code{"A"}.}

\item{dpi}{Resolution for the rendered preview (default \code{96}).}

\item{margin}{A \code{\link[ggplot2:margin]{ggplot2::margin()}} giving the slide margin in \code{"slide"}
mode, in any unit; converted to inches and subtracted from the canvas when
estimating implied dimensions.}

\item{frame}{If \code{TRUE} (default), draw a border around the slide in \code{"slide"}
mode so the canvas edges and content boundaries are visible while
previewing.}

\item{give_dims}{If \code{TRUE} (default), print the implied figure dimensions in
\code{"slide"} mode.}

\item{title, subtitle}{Optional slide title and subtitle text in \code{"slide"}
mode. Note these are not accounted for in the implied-dimension estimate
(see the slide-mode section).}

\item{meta_pt_sizes}{Point sizes for the slide \code{title} and \code{subtitle}, as a
length-2 numeric (default \code{c(24, 16)}).}
}
\value{
Invisibly, the sized object: in \code{"figure"} mode the figure as given;
in \code{"slide"} mode the figure placed on its canvas. Called primarily for the
side effect of opening the rendered preview, and in \code{"slide"} mode for the
printed dimension estimate.
}
\description{
Renders a figure to a temporary file at exact physical dimensions and opens
it, so what you see matches what will be exported — sidestepping the RStudio
plot pane, which rescales figures to its own dimensions and is a poor guide
to how a figure will actually look at its intended size.
}
\details{
All dimensions are in \strong{inches}, matching the units used by the output
devices these previews stand in for (PDF, \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, slides). If
you think in centimetres or millimetres, convert before passing dimensions
(e.g. 10 cm is about 3.94 in). The one exception is \code{margin}, which may be
given in any unit via \code{\link[ggplot2:margin]{ggplot2::margin()}} and is converted to inches for you.

Two modes serve two purposes. In \code{"figure"} mode, the figure is rendered at
the exact \code{width} and \code{height} you specify — use this to judge a figure at
its real export dimensions and dial in a size before saving. In \code{"slide"}
mode, the figure is placed onto a slide-shaped canvas via a layout \code{design},
with an optional margin and title — use this to see how a figure will sit on
a slide, and to get an estimate of the dimensions it should be exported at to
fill its region of that slide.
}
\section{Figure mode — exact sizing}{

\code{mode = "figure"} requires \code{width} and \code{height} in inches and renders the
figure to fill exactly that canvas. This is the tool for "what does this plot
look like at 6 by 4 inches?" — render, look, adjust, repeat, then export at
the size that looked right.
}

\section{Slide mode — placement and dimension estimation}{

\code{mode = "slide"} builds a slide canvas from \code{asp_ratio} and \code{anchor_height}
(slides are conventionally \code{anchor_height} inches tall; width follows the
aspect ratio), places the figure on it according to \code{design}, and reports the
\emph{implied dimensions}: how large the figure itself is within its region of the
slide, after the margin is subtracted. Those implied dimensions are what you
would pass back to \code{mode = "figure"} (or to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}) to export the
figure standalone at the size it occupies on the slide.

The implied-dimension estimate assumes the figure has no slide title or
subtitle. When a \code{title} or \code{subtitle} is supplied, it occupies space the
estimate cannot account for, and \code{view_real_size()} says so. To reserve room
for a title without distorting the estimate, add top \code{margin} rather than a
title: the margin is subtracted exactly, so it gives a clean, controllable
approximation of the space a heading will take.
}

\examples{
\dontrun{
# Figure mode: see a plot at exactly 6 by 4 inches
view_real_size(my_plot, mode = "figure", width = 6, height = 4)

# Slide mode: place a figure in the right two-thirds of a 16:9 slide
# and get the dimensions to export it at
view_real_size(my_plot, mode = "slide", design = "#AA")

# Reserve space for a heading via top margin rather than a title,
# to keep the dimension estimate clean
view_real_size(
  my_plot,
  mode   = "slide",
  margin = ggplot2::margin(1, 0, 0, 0, "in")
)
}

}
\seealso{
\code{\link[=preview_section]{preview_section()}}, which uses slide mode to preview a report section's
composed layout.
}
